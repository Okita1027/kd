import{_ as e}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as o,o as n,b as t}from"./app-BeHCP7Xm.js";const d={},i=t(`<h2 id="dbcontext配置和初始化" tabindex="-1"><a class="header-anchor" href="#dbcontext配置和初始化"><span>DbContext配置和初始化</span></a></h2><h3 id="概述" tabindex="-1"><a class="header-anchor" href="#概述"><span>概述</span></a></h3><h4 id="定义" tabindex="-1"><a class="header-anchor" href="#定义"><span>定义</span></a></h4><p><code>DbContext</code> 是 EF Core 的核心，它扮演着以下几个关键角色：</p><ul><li><strong>数据库会话 (Database Session)</strong>：<code>DbContext</code> 的一个实例代表了与数据库的一次会话或工作单元。在这个会话中，你可以加载实体、跟踪它们的更改、保存数据。</li><li><strong>DbSet 集合</strong>：你的 <code>DbContext</code> 子类会包含 <code>DbSet&lt;TEntity&gt;</code> 属性，每个 <code>DbSet</code> 对应数据库中的一个表，并允许你对该表中的实体执行 LINQ 查询和 CRUD 操作。 <ul><li>例如：<code>public DbSet&lt;Product&gt; Products { get; set; }</code> 意味着你的数据库有一个 <code>Products</code> 表，你可以通过 <code>_context.Products</code> 来访问和操作它。</li></ul></li><li><strong>变更跟踪 (Change Tracking)</strong>：这是 EF Core 最强大的功能之一。<code>DbContext</code> 会自动跟踪从数据库加载的实体以及你添加到它的新实体。当这些实体发生变化时，<code>DbContext</code> 会记录这些变化，并在你调用 <code>SaveChanges()</code> 或 <code>SaveChangesAsync()</code> 时，生成相应的 SQL INSERT、UPDATE 或 DELETE 语句来同步数据库。</li><li><strong>查询和保存数据</strong>：它是你与数据库交互的主要入口点。你可以通过它执行 LINQ 查询、添加新实体、更新现有实体、删除实体，并将这些更改持久化到数据库。</li></ul><h4 id="生命周期" tabindex="-1"><a class="header-anchor" href="#生命周期"><span>生命周期</span></a></h4><p><code>DbContext</code> 实例的生命周期通常是<strong>短暂的</strong>，并且每个工作单元（或每个 HTTP 请求）都应该使用<strong>一个新的</strong> <code>DbContext</code> 实例。这是 EF Core 的设计哲学，也是大多数 Web 应用程序的最佳实践。</p><ul><li><strong>为什么是短暂的？</strong><ul><li><strong>变更跟踪</strong>：一个 <code>DbContext</code> 实例会跟踪它加载的所有实体。如果一个实例存活时间过长，它跟踪的实体数量会不断增加，导致内存占用增多，性能下降。</li><li><strong>并发问题</strong>：<code>DbContext</code> 不是线程安全的。多个线程同时使用同一个 <code>DbContext</code> 实例可能导致数据不一致或其他不可预测的行为。</li><li><strong>数据陈旧</strong>：长时间存活的 <code>DbContext</code> 可能持有旧的数据，无法反映数据库中的最新更改。</li></ul></li><li><strong>如何在 ASP.NET Core 中管理？</strong><ul><li>在 ASP.NET Core 中，<code>DbContext</code> 通常通过<strong>依赖注入 (Dependency Injection - DI)</strong> 进行注册和管理。</li><li>默认情况下，<code>DbContext</code> 会被注册为 <strong><code>Scoped</code> (作用域)</strong> 生命周期。这意味着在每个 HTTP 请求的生命周期内，只会创建一个 <code>DbContext</code> 实例。请求结束，<code>DbContext</code> 实例就会被销毁。这完美符合“每个工作单元使用一个新实例”的最佳实践。</li></ul></li></ul><h4 id="使用方式" tabindex="-1"><a class="header-anchor" href="#使用方式"><span>使用方式</span></a></h4><p><code>DbContext</code> 的配置主要通过重写 <code>OnConfiguring</code> 方法或更常见的通过<strong>依赖注入</strong>在 <code>Program.cs</code> (或 <code>Startup.cs</code>) 中进行。</p><ul><li><p><strong><code>OnConfiguring</code> 方法 (主要用于控制台应用或简单场景)</strong>：</p><div class="language-C# line-numbers-mode" data-ext="C#" data-title="C#"><pre class="language-C#"><code>public class ApplicationDbContext : DbContext
{
    protected override void OnConfiguring(DbContextOptionsBuilder optionsBuilder)
    {
        // 配置使用 SQL Server 数据库，并指定连接字符串
        optionsBuilder.UseSqlServer(&quot;Server=(localdb)\\\\mssqllocaldb;Database=MyDatabase;Trusted_Connection=True;&quot;);
    }
    public DbSet&lt;Product&gt; Products { get; set; }
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li><li><p><strong>通过依赖注入 (<code>Program.cs</code>) (Web 应用推荐)</strong>：</p><div class="language-C# line-numbers-mode" data-ext="C#" data-title="C#"><pre class="language-C#"><code>// Program.cs
builder.Services.AddDbContext&lt;ApplicationDbContext&gt;(options =&gt;
    options.UseMySql(connectionString, ServerVersion.AutoDetect(connectionString)) // 使用 MySQL
    // options.UseSqlServer(connectionString) // 或使用 SQL Server
    // .UseQueryTrackingBehavior(QueryTrackingBehavior.NoTracking) // 可选：配置默认查询行为
);
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>这种方式更加灵活，可以从配置源（如 <code>appsettings.json</code>）获取连接字符串，并且方便地集成到 ASP.NET Core 的整个 DI 体系中。</p></li></ul><blockquote><p>DbContext 非线程安全，仅在单一线程或请求中使用。</p></blockquote><h3 id="上下文池" tabindex="-1"><a class="header-anchor" href="#上下文池"><span>上下文池</span></a></h3><p>“上下文池”是 EF Core 提供的一种<strong>性能优化技术</strong>，它旨在解决创建和初始化 <code>DbContext</code> 实例可能带来的开销，特别是在高并发的 Web 应用程序中。</p><h4 id="池的必要性" tabindex="-1"><a class="header-anchor" href="#池的必要性"><span>池的必要性</span></a></h4><p>虽然 <code>DbContext</code> 实例应该是短暂的，但每次创建新的 <code>DbContext</code> 实例，EF Core 都需要执行一些初始化工作，例如：</p><ul><li><strong>创建新的 <code>DbContext</code> 对象</strong>。</li><li><strong>注册服务</strong>：<code>DbContext</code> 内部有很多服务（如变更跟踪器、查询编译器等），这些服务在 <code>DbContext</code> 首次创建时可能需要初始化。</li><li><strong>模型构建 (Model Building)</strong>：尽管模型构建大部分工作只在应用程序生命周期内执行一次，但某些与 <code>DbContext</code> 实例相关的组件可能仍有少量初始化。</li></ul><p>在高并发环境下，如果每秒钟有成百上千个请求，每次请求都完全从零开始创建一个 <code>DbContext</code> 实例，这些看似微小的开销累计起来也会变得可观。</p><p><strong>上下文池的目标</strong>就是通过<strong>重用现有但空闲的 <code>DbContext</code> 实例</strong>来消除这部分初始化开销，从而提升应用程序的性能和吞吐量。</p><h4 id="工作原理" tabindex="-1"><a class="header-anchor" href="#工作原理"><span>工作原理</span></a></h4><ol><li><strong>创建池</strong>：EF Core 会在应用程序启动时创建一个 <code>DbContext</code> 实例的池子。</li><li><strong>获取实例</strong>：当应用程序需要一个 <code>DbContext</code> 实例时，它会首先尝试从池中获取一个<strong>空闲的、未被使用的实例</strong>。</li><li><strong>重置状态</strong>：从池中取出的 <code>DbContext</code> 实例会被重置到其“干净”状态（即没有实体被跟踪），然后才被应用程序使用。</li><li><strong>归还实例</strong>：当 <code>DbContext</code> 实例被使用完毕（例如，HTTP 请求结束，作用域被销毁），它不会被立即销毁，而是被<strong>归还到池中</strong>，等待下一次重用。</li></ol><h4 id="使用方式-1" tabindex="-1"><a class="header-anchor" href="#使用方式-1"><span>使用方式</span></a></h4><p>在 <code>Program.cs</code>中，将 <code>AddDbContext</code> 替换为 <code>AddDbContextPool</code>：</p><div class="language-C# line-numbers-mode" data-ext="C#" data-title="C#"><pre class="language-C#"><code>// Program.cs
var builder = WebApplication.CreateBuilder(args);

// ... 获取连接字符串 ...

// 启用 DbContext 池
builder.Services.AddDbContextPool&lt;ApplicationDbContext&gt;(options =&gt;
    options.UseMySql(connectionString,
        ServerVersion.AutoDetect(connectionString))
);

// ... 其他服务和应用配置 ...
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h4 id="注意事项" tabindex="-1"><a class="header-anchor" href="#注意事项"><span>注意事项</span></a></h4><ul><li><strong>仅适用于 <code>DbContext</code> 类型具有无参构造函数或只接受 <code>DbContextOptions&lt;TContext&gt;</code> 参数的构造函数。</strong>（这是最常见的，通常不是问题）</li><li><strong>不适用于每个请求都传递配置信息给 <code>DbContext</code> 的场景。</strong> 因为池化的实例是预先配置好的。</li><li><strong>状态管理</strong>：</li><li><ul><li><code>DbContext</code> 从池中取出时会<strong>被重置</strong>，这意味着你<strong>不能</strong>依赖 <code>DbContext</code> 实例在不同请求之间维护任何自定义状态。如果你在 <code>DbContext</code> 中添加了自定义字段并期望它们在重用时保持状态，那将是个问题。</li><li>EF Core 会确保重置其<strong>内部</strong>状态（如变更跟踪器），但如果你有自定义的业务逻辑在 <code>DbContext</code> 构造函数中执行，并且这些逻辑是昂贵的或有副作用的，那么池化可能不会带来太大收益，或者需要额外小心。</li></ul></li><li><strong>并发访问</strong>：池化的 <code>DbContext</code> 实例仍然<strong>不是线程安全的</strong>。每个请求从池中获取一个实例，使用完毕后归还。在单个请求内部，不要在多个线程间共享同一个 <code>DbContext</code> 实例。</li><li><strong>池的大小</strong>：默认情况下，池的大小是有限的。如果并发请求数超出池的容量，新的 <code>DbContext</code> 实例仍然会被创建。你可以通过 <code>options.UsePooledDbContextFactory</code> 来配置池的最大大小（但这通常不是直接通过 <code>AddDbContextPool</code> 的参数）。</li></ul>`,26),s=[i];function c(r,l){return n(),o("div",null,s)}const g=e(d,[["render",c],["__file","dbcontext.html.vue"]]),u=JSON.parse('{"path":"/dotnet/ef%20core/dbcontext.html","title":"DbContext配置和初始化","lang":"zh-CN","frontmatter":{"title":"DbContext配置和初始化","shortTitle":"DbContext配置","description":"DbContext配置和初始化","date":"2025-07-12T17:37:33.000Z","categories":[".NET","EF CORE"],"tags":[".NET"],"order":1},"headers":[{"level":2,"title":"DbContext配置和初始化","slug":"dbcontext配置和初始化","link":"#dbcontext配置和初始化","children":[{"level":3,"title":"概述","slug":"概述","link":"#概述","children":[]},{"level":3,"title":"上下文池","slug":"上下文池","link":"#上下文池","children":[]}]}],"git":{"createdTime":1752655453000,"updatedTime":1752830607000,"contributors":[{"name":"Zhiyun Qin","email":"96156298+Okita1027@users.noreply.github.com","commits":4}]},"readingTime":{"minutes":5.86,"words":1758},"filePathRelative":"dotnet/ef core/dbcontext.md","localizedDate":"2025年7月13日"}');export{g as comp,u as data};
